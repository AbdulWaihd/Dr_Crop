# =============================================================================
# Dr. Crop — frontend (copy to .env.local for local dev)
# Copy:  cp .env.example .env.local   (PowerShell: Copy-Item .env.example .env.local)
# Do NOT put secret LLM/Exa keys here — only public browser-safe vars.
# =============================================================================

# Backend API base URL (no trailing slash). Default matches local FastAPI.
NEXT_PUBLIC_API_URL=http://localhost:8000
